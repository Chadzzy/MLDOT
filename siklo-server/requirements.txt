# SikLo backend — core dependencies.
# These are intentionally LIGHT: the app runs in mock mode and Tier A MT (Qwen
# via Ollama, over HTTP) + edge-tts with only these. Heavy local-STT deps
# (funasr/torch) live in requirements-tier-a.txt so `pip install -r
# requirements.txt` is fast and succeeds anywhere.

fastapi>=0.111,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.7,<3.0
pydantic-settings>=2.3,<3.0
python-multipart>=0.0.9
httpx>=0.27,<1.0

# TTS (Tier A default) — free, no API key. Reaches Microsoft over the network.
edge-tts>=6.1,<8.0

# Deterministic romanization for Go cards / MT fallback.
ToJyutping>=3.0
pypinyin>=0.51

# Convenience wrapper around ffmpeg (the audio service also shells out directly).
ffmpeg-python>=0.2.0

# Tests
pytest>=8.0
pytest-asyncio>=0.23
